pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo env.BRANCH_NAME
                     checkout scm
                 }
                 }
      }
}